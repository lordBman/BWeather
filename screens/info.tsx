import { View } from "react-native";
import { RootScreenProps } from "../utils/types";

const Info: React.FC<RootScreenProps<"info">> = () =>{
    return (
        <View>
            
        </View>
    );
}

export default Info;